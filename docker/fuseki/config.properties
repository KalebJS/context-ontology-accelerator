# Jena Fuseki — local Neptune replacement (SPARQL endpoint).
# Dataset "coa" created at first boot; no auth on the internal network.
[server]
adminPassword=coa-admin

[main]
# The coa dataset is created by the entrypoint (config-dataset.ttl is applied
# by the fuseki docker image's /fuseki configuration directory mount).
